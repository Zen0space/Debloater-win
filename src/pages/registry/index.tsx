import { CategoryPage } from '../../components/cards/CategoryPage';

export default function Registry() {
  return (
    <CategoryPage
      title="Registry Tweaks"
      description="Apply registry modifications for Windows customization"
      category="registry"
    />
  );
}
